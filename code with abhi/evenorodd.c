#include<stdio.h>
int main ()
{
    int n;
    printf("Enter any number(*_*)\n\n");
    scanf("%d",&n);
    if(n%2==0)
    {
        printf("number is (*_*) even");

    }
    else 
    {
        printf("number is (*-*) odd");

    }
    return 0;
}